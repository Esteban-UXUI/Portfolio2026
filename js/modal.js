/* ============================================
   MODAL — removed (language now in nav)
   Kept as empty module to avoid import errors
   ============================================ */
export function initModal() {}
